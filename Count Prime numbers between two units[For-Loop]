#include<stdio.h>
void prime()
{
	int i,j,c,n;
	printf("Enter element max to be counted for prime?");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		c=0;
		for(j=1;j<=i;j++)
		{
			if(i%j==0)
				c++;
		}
		if(c==2)
			printf("%d\n",i);
	}
}
int main()
{
	prime();
}
